<nav class="navbar navbar-fixed-top navbar-inverse">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to root_path, class: 'navbar-brand' do %>
          TRUCKING
      <% end %>
    </div>

    <div class="collapse navbar-collapse" id="navbar-collapse">
      <% unless @project_version.blank? %>
        <ul class="nav navbar-nav">
          <li><span class="label label-info">version <%= @project_version %></span></li>
        </ul>
      <% end %>
      <ul class="navbar-nav navbar-right">
        <% if user_signed_in? %>
            <p class="navbar-text">
              <%= t 'hello'%>, <%= link_to current_user.phone, class: 'navbar-link' %>
            </p>
            <ul class="nav navbar-nav">
              <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%= t 'account' %> <span class="caret"></span></a>
                <ul class="dropdown-menu">
                  <li><%= link_to (t 'edit_ptofile'), edit_user_registration_path(current_user) %></li>
                  <li class="divider"></li>
                  <li><%= link_to (t 'sign_out'), destroy_user_session_path, method: :delete %></li>
                </ul>
              </li>
            </ul>
        <% else %>
            <ul class="nav navbar-nav">
              <li><%= link_to (t 'sign_up'), new_user_registration_path, id: 'sign_up' %></li>
              <li><%= link_to (t 'sign_in'), new_user_session_path, id: 'log_in' %></li>
            </ul>
        <% end %>
    </div>
  </div>
</nav>
