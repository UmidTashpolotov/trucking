<h3>Оставьте комментарий о <%= @order.user.name %>:</h3>
<%= simple_form_for [@order, @comment] do |f| %>
    <%= f.input :body %>
    <%= f.input :rating, collection: 1..5, default: 1 %>
    <%= f.submit %>
<% end %>